diff --git a/include/boost/multi/adaptors/cuda/cublas/context.hpp b/include/boost/multi/adaptors/cuda/cublas/context.hpp
index 1448c63eb..3bf73bb35 100644
--- a/include/boost/multi/adaptors/cuda/cublas/context.hpp
+++ b/include/boost/multi/adaptors/cuda/cublas/context.hpp
@@ -137,16 +137,17 @@ template<class T, std::enable_if_t<is_z<T>{}, int> =0> constexpr auto data_cast(
 
 class context : private std::unique_ptr<typename std::pointer_traits<hicu(blasHandle_t)>::element_type, decltype(&hicu(blasDestroy))> {
 	using pimpl_t = std::unique_ptr<typename std::pointer_traits<hicu(blasHandle_t)>::element_type, decltype(&hicu(blasDestroy))>;
+	bool sync_after_call_ = true;
 	hicup(Stream_t) stream() const {hicup(Stream_t) streamId; cuda::cublas::call<hicu(blasGetStream)>(this->get(), &streamId); return streamId;}
 	template<auto Function, class... Args>
 	void sync_call(Args... args) const {
 		call<Function>(const_cast<context*>(this)->get(), args...);
-		this->synchronize();
+		if(sync_after_call_) this->synchronize();
 	}
 	template<auto Function, class... Args>
 	void sync_call(Args... args) {
 		call<Function>(this->get(), args...);
-		this->synchronize();
+		if(sync_after_call_) this->synchronize();
 	}
 
  public:
@@ -156,14 +157,13 @@ class context : private std::unique_ptr<typename std::pointer_traits<hicu(blasHa
 		return ctxt;
 	};
 	context() : pimpl_t{[] {hicu(blasHandle_t) h; hicu(blasCreate)(&h); return h;}(), &hicu(blasDestroy)} {}
-	// using ssize_t = int;
-	// static int version() {int ret; cuda::cublas::call<hicu(blasGetVersion)>(nullptr, &ret); return ret;} // no hipblasGetVersion available
+	void set_stream(hicup(Stream_t) s) { cuda::cublas::call<hicu(blasSetStream)>(this->get(), s); }
+	void set_sync(bool sync) { sync_after_call_ = sync; }
 	void synchronize() const {
-		// cudaError_t e = cudaDeviceSynchronize();
-		auto s = stream();
-		if(s != 0) {throw std::logic_error("CUBLAS stream expected to be zero");}
-		hicup(Error_t) e = hicup(StreamSynchronize)(s);
-		if(e != hicup(Success)) {throw std::runtime_error{"cannot synchronize stream in cublas context"};}
+		hicup(Error_t) e = hicup(DeviceSynchronize)();
+		if(e != hicup(Success)) {
+			throw std::runtime_error{std::string("cannot synchronize in cublas context: ") + hicup(GetErrorString)(e)};
+		}
 	}
 
 	template<
@@ -210,6 +210,7 @@ class context : private std::unique_ptr<typename std::pointer_traits<hicu(blasHa
 		std::enable_if_t<std::is_convertible_v<XP, ::thrust_hicup::pointer<X>> and std::is_convertible_v<YP, ::thrust_hicup::pointer<Y>>, int> = 0
 	>
 	void axpy(SSize n, ALPHA const* alpha, XP x, SSize incx, YP y, SSize incy) {
+		if(is_s<X>{}) { sync_call<hicu(blasSaxpy)>(static_cast<int>(n), (float         const*)alpha, (float         const*)raw_pointer_cast(x), static_cast<int>(incx), (float        *)raw_pointer_cast(y), static_cast<int>(incy)); }
 		if(is_d<X>{}) { sync_call<hicu(blasDaxpy)>(static_cast<int>(n), (double        const*)alpha, (double        const*)raw_pointer_cast(x), static_cast<int>(incx), (double       *)raw_pointer_cast(y), static_cast<int>(incy)); }
 		if(is_z<X>{}) { sync_call<hicu(blasZaxpy)>(static_cast<int>(n), (DoubleComplex const*)alpha, (DoubleComplex const*)raw_pointer_cast(x), static_cast<int>(incx), (DoubleComplex*)raw_pointer_cast(y), static_cast<int>(incy)); }
 	}
@@ -229,7 +230,9 @@ class context : private std::unique_ptr<typename std::pointer_traits<hicu(blasHa
 	>
 	void gemm(char transA, char transB, SSize m, SSize n, SSize k, ALPHA const* alpha, AAP aa, SSize lda, BBP bb, SSize ldb, BETA const* beta, CCP cc, SSize ldc) {
 		/*MULTI_MARK_SCOPE("cublasXgemm");*/
+		if(is_s<AA>{}) {sync_call<hicu(blasSgemm)>(cuda::cublas::operation{transA}, cuda::cublas::operation{transB}, static_cast<int>(m), static_cast<int>(n), static_cast<int>(k), (float         const*)alpha, (float         const*)::thrust::raw_pointer_cast(aa), static_cast<int>(lda), (float         const*)::thrust::raw_pointer_cast(bb), static_cast<int>(ldb), (float         const*)beta, (float        *)::thrust::raw_pointer_cast(cc), static_cast<int>(ldc));}
 		if(is_d<AA>{}) {sync_call<hicu(blasDgemm)>(cuda::cublas::operation{transA}, cuda::cublas::operation{transB}, static_cast<int>(m), static_cast<int>(n), static_cast<int>(k), (double        const*)alpha, (double        const*)::thrust::raw_pointer_cast(aa), static_cast<int>(lda), (double        const*)::thrust::raw_pointer_cast(bb), static_cast<int>(ldb), (double        const*)beta, (double       *)::thrust::raw_pointer_cast(cc), static_cast<int>(ldc));}
+		if(is_c<AA>{}) {sync_call<hicu(blasCgemm)>(cuda::cublas::operation{transA}, cuda::cublas::operation{transB}, static_cast<int>(m), static_cast<int>(n), static_cast<int>(k), (hicu(Complex)  const*)alpha, (hicu(Complex)  const*)::thrust::raw_pointer_cast(aa), static_cast<int>(lda), (hicu(Complex)  const*)::thrust::raw_pointer_cast(bb), static_cast<int>(ldb), (hicu(Complex)  const*)beta, (hicu(Complex) *)::thrust::raw_pointer_cast(cc), static_cast<int>(ldc));}
 		if(is_z<AA>{}) {sync_call<hicu(blasZgemm)>(cuda::cublas::operation{transA}, cuda::cublas::operation{transB}, static_cast<int>(m), static_cast<int>(n), static_cast<int>(k), (DoubleComplex const*)alpha, (DoubleComplex const*)::thrust::raw_pointer_cast(aa), static_cast<int>(lda), (DoubleComplex const*)::thrust::raw_pointer_cast(bb), static_cast<int>(ldb), (DoubleComplex const*)beta, (DoubleComplex*)::thrust::raw_pointer_cast(cc), static_cast<int>(ldc));}
 	}
 
diff --git a/include/boost/multi/detail/adl.hpp b/include/boost/multi/detail/adl.hpp
index 8d7254692..2ec9db55b 100644
--- a/include/boost/multi/detail/adl.hpp
+++ b/include/boost/multi/detail/adl.hpp
@@ -316,7 +316,7 @@ class adl_uninitialized_copy_t {
 #endif
 		return std::uninitialized_copy(std::move(first), std::move(last), std::move(d_first));
 	}
-#if defined(__CUDACC__) || defined(__HIPCC__)
+#if (defined(__CUDACC__) || defined(__HIPCC__)) && !defined(CCCL_VERSION) // CCCL 2.x moved thrust::uninitialized_copy
 	template<class InIt, class FwdIt, class ValueType = typename std::iterator_traits<FwdIt>::value_type>
 	constexpr auto _(priority<2>/**/, InIt first, InIt last, FwdIt d_first) const -> decltype(::thrust::uninitialized_copy(first, last, d_first))  // doesn't work with culang 17, cuda 12 ?
 	{
